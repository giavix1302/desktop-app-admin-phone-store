<Window x:Class="AdminPhoneStore.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AdminPhoneStore"
        mc:Ignorable="d"
        Title="Admin Phone Store"
        Height="600"
        Width="1000"
        WindowStartupLocation="CenterScreen">
    <!-- DataContext được set trong MainWindow.xaml.cs thông qua DI -->

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- MENU -->
        <StackPanel Background="#2C2F33">
            <Button Content="Dashboard"
                    Command="{Binding ShowDashboardCommand}"
                    Margin="10"/>
            <Button Content="Product"
                    Command="{Binding ShowProductCommand}"
                    Margin="10"/>
            <Button Content="← Back"
                    Command="{Binding GoBackCommand}"
                    Margin="10"/>
        </StackPanel>

        <!-- NỘI DUNG -->
        <ContentControl Grid.Column="1"
                        Content="{Binding CurrentView}"/>
    </Grid>
</Window>
